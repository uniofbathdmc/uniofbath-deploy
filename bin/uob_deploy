#!/usr/bin/env ruby

require 'uob_deploy'

if ARGV[0] == "init"
  UobDeploy.init
elsif ARGV[0] == "new"
  UobDeploy.new
else
  puts "Usage:"
  puts "init -- create deploy.rb and environment setup"
  puts "new -- create a new environment file"
end
